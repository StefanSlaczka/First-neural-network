/*
    Thing I need to do.

    step one
    
        Get input from Attention Mechanism
            words, scores[][], bias

        for (int i = 0; i < inputs.lenght; i++){
            sum += inputs[i] * weights[i];
        }
        return sum + bias;

    step 2
        case "sigmoid":
            return  / ()  + Math.exp(-z))
        case "relu":
            return  Math.max(0, z)
        case "tanh":
            return  Math.tanh(z)
        case "linear":
            return  z
        defualt:

    step 3 print all
            
*/